# Crime-Report-System

nslookup generativelanguage.googleapis.com

Test-NetConnection generativelanguage.googleapis.com -Port 443

python -c "import socket; print(socket.gethostbyname('generativelanguage.googleapis.com'))"

python -c "import httpx; r=httpx.get('https://generativelanguage.googleapis.com', timeout=10); print(r.status_code)"
